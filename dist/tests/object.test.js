"use strict";
describe('bject', function () {
    it('should support in typescript', function () {
        const person = {
            id: "1",
            name: "Riski"
        };
        console.info(person);
        // console.info(product)
    });
});
